# Test info

- Name: TC005_@sanity_DCI Blog Page_Validate the Pagination and Filter Functionalities on the DCI Blog page for Desktop/Mobile  
- Location: /builds/vendor_deloitte/dci/DCI_Automation/tests/DCI_Blog_Landing/DCI_Blog_Landing.spec.ts:115:5

# Error details

```
Error: locator.textContent: Test timeout of 200000ms exceeded.
Call log:
  - waiting for locator('//div[@class=\'archive-results\']/header/p')

    at DCI_Blog_Page.getCountSummaryText (/builds/vendor_deloitte/dci/DCI_Automation/pages/DCI_Blog_Page.ts:119:40)
    at /builds/vendor_deloitte/dci/DCI_Automation/tests/DCI_Blog_Landing/DCI_Blog_Landing.spec.ts:123:45
```

# Test source

```ts
   19 |     readonly filterCountSummary: Locator;
   20 |     readonly nextButton: Locator;
   21 |     readonly prevButton: Locator;
   22 |     readonly pageNumberButtons: Locator;
   23 |     readonly filterResults_mobile: Locator;
   24 |     readonly enterSearchTerm_mobile: Locator;
   25 |     readonly searchFilter: Locator;
   26 |     readonly selectCategory_mobile: Locator;
   27 |     readonly applyfilter_mobile: Locator;
   28 |     readonly clearfilters_mobile: Locator;
   29 |     readonly filterCloseButton_desktop: Locator;
   30 |     readonly category_label: Locator;
   31 |     readonly clearSearchIcon: Locator;
   32 |     readonly loadingSpinner: Locator;
   33 |     readonly noResultsMatchingMessage: Locator;
   34 |     readonly mobileFilterCloseButton: Locator;
   35 |
   36 |
   37 |
   38 |
   39 |     constructor(page: Page, isMobile: boolean | undefined) {
   40 |         this.page = page;
   41 |         this.isMobile = isMobile;
   42 |         this.heading = page.locator('h1', { hasText: 'DCI Blog' });
   43 |         this.contentSection = page.locator("//div[@class='archive-body-content']/descendant::p");
   44 |         //this.ctaButton = page.getByRole('link', { name: 'CTA' });
   45 |         this.ctaButton = page.locator("//div[@class='archive-hero-button-containter']/a");
   46 |
   47 |         this.searchLabel = page.locator('h2', { hasText: 'Search' });
   48 |         this.searchBar = page.getByPlaceholder("Enter search term");
   49 |         this.searchFilter = page.locator("//button[contains(@aria-label, 'Search')]");
   50 |         this.categoriesLabel = page.locator('h2', { hasText: 'Categories' });
   51 |         this.categoriesDropdown = page.locator("//span[contains(@class,'p-dropdown-label')  and text()='Select Category']");
   52 |         this.filterLabels = page.locator('.filters label, .filter-label'); 
   53 |         //this.blogTiles = page.locator('.views-row, .blog-tile, .card'); 
   54 |         this.pagination = page.locator('.pagination, nav[aria-label*="Pagination"], .pager');
   55 |         this.filterCountSummary = page.locator("//div[@class='archive-results']/header/p");
   56 |         this.blogTiles = page.locator("//article[@class='content card blog-page blog--card']");
   57 |         this.nextButton = this.page.getByRole('link', { name: 'Next' });
   58 |         this.prevButton = this.page.getByRole('link', { name: ' Previous ' });
   59 |         this.pageNumberButtons = this.page.locator("//li[contains(@class,' page-no')]");
   60 |         this.filterResults_mobile = page.getByRole('button', { name: 'Filter Results' });
   61 |         this.enterSearchTerm_mobile = page.locator("//input[@placeholder='Enter search term']");
   62 |         this.searchFilter = page.locator("//button[contains(@aria-label, 'Search')]");
   63 |         this.selectCategory_mobile = page.locator('span').filter({ hasText: 'Select Category' });
   64 |         this.applyfilter_mobile = page.getByRole("button", { name: "Apply" });
   65 |         this.clearfilters_mobile = page.getByRole("button", { name: "Clear Filters" });
   66 |         this.filterCloseButton_desktop = page.locator("//span[@aria-label='Clear']");
   67 |         this.category_label = page.locator("//div[@id='category-dropdown']/span");
   68 |         this.clearSearchIcon = this.page.locator("//button[@class='clear-results']");
   69 |         this.loadingSpinner = this.page.locator("//i[contains(@class,'icon-duke-common-spinner')]");
   70 |         this.noResultsMatchingMessage = this.page.getByText("There are no results matching your query.");
   71 |         this.mobileFilterCloseButton = page.locator("//i[@class='fas fa-times']");
   72 |     }   
   73 |
   74 |
   75 |     async selectCategory(category: string) {
   76 |         await this.categoriesDropdown.selectOption({ label: category });
   77 |     }
   78 |
   79 |
   80 |     async getFontAndColor(locator: Locator) {
   81 |         const font = await locator.evaluate(el => window.getComputedStyle(el).fontFamily);
   82 |         const size = await locator.evaluate(el => window.getComputedStyle(el).fontSize);
   83 |         const color = await locator.evaluate(el => window.getComputedStyle(el).color);
   84 |         return { font, size, color };
   85 |     }
   86 |
   87 |     async getTileCount() {
   88 |         return await this.blogTiles.count();
   89 |     }
   90 |
   91 |     async getTileElements() {
   92 |         return this.blogTiles;
   93 |     }
   94 |
   95 |     async getTileTitle(tile: Locator) {
   96 |         return tile.locator('h3 a').first();
   97 |     }
   98 |
   99 |     async getTileDate(tile: Locator) {
  100 |         return tile.locator('.date, time').first();
  101 |     }
  102 |
  103 |     async getTileNewsOutlet(tile: Locator) {
  104 |         return tile.locator('.news-outlet-cls').first();
  105 |     }
  106 |
  107 |     async getTileType(tile: Locator) {
  108 |         //return tile.locator('.article-type, .news-mention, .tile-tag').first();
  109 |         return tile.locator('.event-type').first();
  110 |     }
  111 |
  112 |     async getPaginationButtons() {
  113 |         return this.pagination.locator('a, button');
  114 |     }
  115 |
  116 |     /** Returns the summary text, e.g., "1-12 of 231 Clinical Trials" */
  117 |     async getCountSummaryText() {
  118 |         //return this.page.textContent('[data-testid="clinical-trial-count-summary"]'); // Update selector as needed
> 119 |         return this.filterCountSummary.textContent();
      |                                        ^ Error: locator.textContent: Test timeout of 200000ms exceeded.
  120 |     }
  121 |
  122 |     /** Returns the total count from the summary text */
  123 |     async getTotalCountFromSummary() {
  124 |         const summary = await this.getCountSummaryText();
  125 |         const match = summary?.match(/of\s+(\d+)\s+blogs/i);
  126 |         return match ? parseInt(match[1], 10) : null;
  127 |     }
  128 |
  129 |
  130 |     async isPaginationVisible() {
  131 |         return await this.pagination.isVisible();
  132 |     }
  133 |
  134 |
  135 |     async clickNext() {
  136 |         await this.nextButton.click();
  137 |     }
  138 |
  139 |
  140 |     async clickPrevious() {
  141 |         await this.prevButton.click();
  142 |     }
  143 |
  144 |     async clickPageNumber(index: number) {
  145 |         await this.pageNumberButtons.nth(index).click();
  146 |     }
  147 |
  148 |     // In your page object
  149 |     async waitForPaginationVisible(timeout = 5000) {
  150 |         await this.pagination.waitFor({ state: 'visible', timeout });
  151 |     }
  152 |
  153 |     // Enter search keyword and clickOnEnter
  154 |     async enterSearchTermAndClickEnter(searchTerm: string) {
  155 |         await this.searchBar.fill(searchTerm);
  156 |         await this.searchBar.press('Enter');
  157 |         await waitForResultsLoaded(this.page, this.loadingSpinner);
  158 |     }
  159 |
  160 |
  161 |     // Close Mobile filter
  162 |     async closeMobileFilter(){
  163 |         await this.mobileFilterCloseButton.click();
  164 |     }
  165 |
  166 |
  167 |     /**
  168 |    * Verifies all blog tiles for visibility, clickability, type, date, and news outlet.
  169 |    * @param validTypes Array of valid type tags (default: ['Article', 'News Mention'])
  170 |    */
  171 |     async verifyAllBlogTiles(validTypes: string[] = ['Article', 'News Mention']) {
  172 |         const tiles = await this.getTileElements();
  173 |         const count = await tiles.count();
  174 |         for (let i = 0; i < count; i++) {
  175 |             const tile = tiles.nth(i);
  176 |
  177 |             // Title is clickable
  178 |             const title = await this.getTileTitle(tile);
  179 |             await expect(title).toBeVisible();
  180 |             const hasHref = await title.getAttribute('href');
  181 |             const hasOnclick = await title.getAttribute('onclick');
  182 |             expect(hasHref !== null || hasOnclick !== null).toBeTruthy();
  183 |
  184 |             // Type tag
  185 |             const type = await this.getTileType(tile);
  186 |             await expect(type).toBeVisible();
  187 |             const eventType = await type.textContent();
  188 |             expect(validTypes).toContain(eventType);
  189 |
  190 |             // Date
  191 |             const date = await this.getTileDate(tile);
  192 |             await expect(date).toBeVisible();
  193 |
  194 |             // News Outlet
  195 |             const outlet = await this.getTileNewsOutlet(tile);
  196 |             await expect(outlet).toBeVisible();
  197 |         }
  198 |     }
  199 |
  200 |
  201 |     // Get search Related results and verify if the Title displayed on the cards are correct
  202 |
  203 |     async validateSearchRelatedResults(keyword: string) {
  204 |         // 2. Wait for results to load
  205 |         await waitForResultsLoaded(this.page, this.loadingSpinner);
  206 |         // 3. Get all visible card titles
  207 |         const titles = await this.getAllVisibleCardTitles();
  208 |         // 4. Assert each title contains the keyword (case-insensitive)
  209 |         for (const title of titles) {
  210 |             const normalizedTitle = normalizeSpecialCharacters(title);
  211 |             expect(normalizedTitle).toContain(keyword.toLowerCase());
  212 |         }
  213 |     }
  214 |
  215 |     /**
  216 | * Applies a filter and validates the summary count.
  217 | * @param filterName - The filter to apply (e.g., "Search", "Categories", etc.)
  218 | * @param value - The value to select or enter
  219 | * @param total - The original total count to compare against
```