# Test info

- Name: TC013_@sanity_Clinical Trial Landing Page_Validate the working of Category filter on clinical trial landing page
- Location: C:\Deloitte_Duke_Project\Duke_Cancer_Institute_Automation\tests\Clinical_Trial_Landing\Clinical_Trial_Landing.spec.ts:429:5

# Error details

```
Error: locator.click: Test timeout of 220000ms exceeded.
Call log:
  - waiting for getByRole('button', { name: 'Filter Results' })

    at C:\Deloitte_Duke_Project\Duke_Cancer_Institute_Automation\tests\Clinical_Trial_Landing\Clinical_Trial_Landing.spec.ts:436:64
```

# Page snapshot

```yaml
- link "Skip to main content":
  - /url: "#main-content"
- region "header":
  - heading "Duke Cancer Institute" [level=1]
- banner:
  - link "Duke Cancer Institute Logo":
    - /url: /
    - img "Duke Cancer Institute Logo"
  - button "MENU"
- main:
  - navigation "Breadcrumbs navigation":
    - list:
      - listitem:
        - link "Home":
          - /url: /
          - text: 
  - heading "Clinical Trials" [level=1]
  - paragraph: Our experts across all cancer specialties participate in national clinical trials and perform the most advanced treatment methods using the latest technology available. As a result, we offer a broader selection of medical, surgical and minimally invasive options to more people, including those who are not considered candidates for treatment elsewhere. We offer unprecedented access to clinical trials.
- contentinfo:
  - img "Duke Cancer Institute"
  - link "Visit us on Facebook":
    - /url: https://www.facebook.com/DukeCancerInstitute/
    - img "facebook-icon icon":
      - img "Facebook"
  - link "Visit us on Instagram":
    - /url: https://www.instagram.com/dukecancerinstitute
    - img "instagram icon":
      - img
  - link "Visit us on X":
    - /url: https://twitter.com/dukecancer
    - img "x-icon icon":
      - img
  - link "Visit us on Youtube":
    - /url: https://www.youtube.com/user/dukecancerinstitute
    - img "youtube-icon icon":
      - img "YouTube"
  - link "Subscribe to our RSS Feed":
    - /url: https://www.dukecancerinstitute.org/rss.xml
    - img "rss-icon icon":
      - img "RSS"
  - navigation "Sub Footer navigation":
    - list:
      - listitem:
        - link "Website Privacy Policy":
          - /url: https://www.dukehealth.org/privacy/website-privacy-policy
      - listitem:
        - link "Sitemap":
          - /url: /sitemap.xml
      - listitem:
        - 'link "Leading with Heart: Serving Humanity"':
          - /url: https://www.dukehealth.org/leading-with-heart-serving-humanity-pledge
      - listitem:
        - link "Notice of Nondiscrimination":
          - /url: https://www.dukehealth.org/notice-of-nondiscrimination
      - listitem:
        - link "Notice of Privacy Practices":
          - /url: https://www.dukehealth.org/privacy
      - listitem:
        - link "Terms and Conditions":
          - /url: https://www.dukehealth.org/terms-and-conditions-of-use
      - listitem:
        - link "Accessibility":
          - /url: https://accessibility.duke.edu/
      - listitem: © DUKE CANCER INSTITUTE 2025
```

# Test source

```ts
  336 |     };
  337 |
  338 |     // 1. Navigate to Clinical Trial Landing Page
  339 |     await homepage.navigateToClinicalTrialLandingPage();
  340 |     await expect(page.url()).toContain("/clinical-trials");
  341 |     await openMobileFilterPanel();
  342 |
  343 |     // Validate "Open for Enrollment"
  344 |     await clinical_Trial_Landing_Page.validateEnrollmentStatusFilter('Open for Enrollment', 'Open for Enrollment');
  345 |     await resetFilters();
  346 |
  347 |     // Wait before next filter (if needed)
  348 |     await page.waitForTimeout(5000);
  349 |
  350 |     // Open filter panel again for mobile
  351 |     await openMobileFilterPanel();
  352 |
  353 |     // Validate "Open for Enrollment Soon"
  354 |     await clinical_Trial_Landing_Page.validateEnrollmentStatusFilter('Open for Enrollment Soon', 'Open for Enrollment Soon');
  355 |     await resetFilters();
  356 | });
  357 |
  358 |
  359 | test('TC012_@sanity_Clinical Trial Landing Page_Validate the UI of Category filter on clinical trial landing page in desktop view/iPad view/mobile view', async ({ page, beforeHook, afterHook, homepage, clinical_Trial_Landing_Page, testData, isMobile }) => {
  360 |     // 1. Navigate to Clinical Trial Landing Page
  361 |     await homepage.navigateToClinicalTrialLandingPage();
  362 |     await expect(page.url()).toContain("/clinical-trials");
  363 |     // Open filter panel on mobile 
  364 |     if (isMobile) {
  365 |         await clinical_Trial_Landing_Page.filterResults_mobile.click();
  366 |     }
  367 |     // 2. Check h2 "CATEGORIES" text and styles
  368 |     const header = clinical_Trial_Landing_Page.categoriesHeader; // Locator for categories header
  369 |     await expect(header).toHaveText('CATEGORIES');
  370 |     const headerStyles = await header.evaluate(el => ({
  371 |         fontWeight: window.getComputedStyle(el).fontWeight,
  372 |         textTransform: window.getComputedStyle(el).textTransform,
  373 |         color: window.getComputedStyle(el).color
  374 |     }));
  375 |     expect(headerStyles.fontWeight).toBe('700');
  376 |     expect(headerStyles.textTransform.toLowerCase()).toBe('uppercase');
  377 |     expect(headerStyles.color).toBe('rgb(38, 38, 38)'); // #262626
  378 |     // 3. Check placeholder for category filter
  379 |     const dropdownInput = clinical_Trial_Landing_Page.categoryDropdownInput;
  380 |     const placeholder = await dropdownInput.textContent();
  381 |     expect(placeholder?.toLowerCase()).toContain('select category');
  382 |     await expect(dropdownInput).toBeVisible();
  383 |     // 4. Expand dropdown and check for search filter inside dropdown
  384 |     await dropdownInput.click();
  385 |     const dropdownPanel = clinical_Trial_Landing_Page.categoryDropdownPanel; // Locator for dropdown panel
  386 |     await expect(dropdownPanel).toBeVisible();
  387 |     // 4.1 Check for search filter inside dropdown
  388 |     const searchInput = clinical_Trial_Landing_Page.categoryDropdownSearchInput; // Locator for search input inside dropdown
  389 |     await expect(searchInput).toBeVisible();
  390 |     // 4.2 Check working of search filter inside dropdown
  391 |     //const testCategory = 'Gynecologic Cancer'; // Use a real category from your data
  392 |     await searchInput.fill(testData.Clinical_trials_landing_page_Categories);
  393 |     const filteredOption = clinical_Trial_Landing_Page.categoryDropdownOption(testData.Clinical_trials_landing_page_Categories);
  394 |     await expect(filteredOption).toBeVisible();
  395 |     await expect(filteredOption).toHaveText(testData.Clinical_trials_landing_page_Categories);
  396 |     // 4.4 Check hover state on dropdown option
  397 |     if (!isMobile) {
  398 |         await filteredOption.hover();
  399 |         const hoverBg = await filteredOption.evaluate(el => window.getComputedStyle(el).color);
  400 |         expect(hoverBg).not.toBe('rgba(0, 0, 0, 0)'); // Should change on hover
  401 |     }
  402 |     // 4.5 Select the option and check if it appears on the filter
  403 |     await filteredOption.click();
  404 |     const selectedText = await clinical_Trial_Landing_Page.selectCategoryInput.textContent();
  405 |     expect(selectedText?.toLowerCase()).toContain(testData.Clinical_trials_landing_page_Categories.toLowerCase());
  406 |     // 5. Collapse dropdown by clicking outside
  407 |     if (!isMobile) {
  408 |         await page.click('body', { position: { x: 0, y: 0 } });
  409 |         await expect(dropdownPanel).toBeHidden();
  410 |     }
  411 |
  412 |     if (!isMobile) {
  413 |         await clinical_Trial_Landing_Page.clickOnCloseButton_resetFilter();
  414 |         await dropdownInput.click();
  415 |         await searchInput.fill(testData.Clinical_trials_landing_page_search_term_keyword_with_multiple_results);
  416 |         await page.waitForTimeout(2000);
  417 |         const dropdownScrollPanel = clinical_Trial_Landing_Page.DropDownScrollPanel;
  418 |         //console.log("Drop down panel height "+dropdownPanel1.scr+" Client Height "+)
  419 |         const hasScroll = await dropdownScrollPanel.evaluate(el => el.scrollHeight > el.clientHeight);
  420 |         expect(hasScroll).toBe(true);
  421 |     }
  422 | });
  423 |
  424 |
  425 |
  426 |
  427 |
  428 |
  429 | test('TC013_@sanity_Clinical Trial Landing Page_Validate the working of Category filter on clinical trial landing page', async ({ page, beforeHook, afterHook, homepage, clinical_Trial_Landing_Page, testData, isMobile }) => {
  430 |     // 1. Navigate to Clinical Trial Landing Page
  431 |     await homepage.navigateToClinicalTrialLandingPage();
  432 |     await expect(page.url()).toContain("/clinical-trials");
  433 |
  434 |     // Open filter panel on mobile if needed
  435 |     if (isMobile) {
> 436 |         await clinical_Trial_Landing_Page.filterResults_mobile.click();
      |                                                                ^ Error: locator.click: Test timeout of 220000ms exceeded.
  437 |     }
  438 |     const categoryToTest = testData.Clinical_trials_landing_page_Categories;
  439 |     // Validate category filter
  440 |     //await clinical_Trial_Landing_Page.validateCategoryFilter(categoryToTest);
  441 |     await clinical_Trial_Landing_Page.validateCategoryFilterWWithoutIteration(categoryToTest);
  442 | });
  443 |
  444 |
  445 |
  446 | test('TC014_@sanity_Clinical Trial Landing Page_Validate the UI of Age group filter on clinical trial landing page in desktop view/iPad view/mobile view', async ({ page, beforeHook, afterHook, homepage, clinical_Trial_Landing_Page, testData, isMobile }) => {
  447 |     // 1. Navigate to Clinical Trial Landing Page
  448 |     await homepage.navigateToClinicalTrialLandingPage();
  449 |     await expect(page.url()).toContain("/clinical-trials");
  450 |
  451 |     // Open filter panel on mobile if needed
  452 |     if (isMobile) {
  453 |         await clinical_Trial_Landing_Page.filterResults_mobile.click();
  454 |     }
  455 |
  456 |     // 2. Check h2 "AGE GROUP" text and styles
  457 |     const header = clinical_Trial_Landing_Page.ageGroupHeader;
  458 |     await expect(header).toHaveText('Age Group');
  459 |     const headerStyles = await header.evaluate(el => ({
  460 |         fontWeight: window.getComputedStyle(el).fontWeight,
  461 |         textTransform: window.getComputedStyle(el).textTransform,
  462 |         color: window.getComputedStyle(el).color
  463 |     }));
  464 |     expect(headerStyles.fontWeight).toBe('700');
  465 |     expect(headerStyles.textTransform.toLowerCase()).toBe('uppercase');
  466 |     expect(headerStyles.color).toBe('rgb(38, 38, 38)'); // #262626
  467 |
  468 |     // 3. Check placeholder for age group filter
  469 |     const dropdownInput = clinical_Trial_Landing_Page.ageGroupDropdownInput;
  470 |     const placeholder = await dropdownInput.textContent();
  471 |     expect(placeholder?.toLowerCase()).toContain('filter by age group');
  472 |     await expect(dropdownInput).toBeVisible();
  473 |
  474 |     // 4. Expand dropdown and check for options
  475 |     await dropdownInput.click();
  476 |     const dropdownPanel = clinical_Trial_Landing_Page.ageGroupDropdownPanel;
  477 |     await expect(dropdownPanel).toBeVisible();
  478 |
  479 |     // 4.1 Check for both options
  480 |     const adultsOption = clinical_Trial_Landing_Page.ageGroupDropdownOption('Adults');
  481 |     const bothOption = clinical_Trial_Landing_Page.ageGroupDropdownOption('Both');
  482 |     await expect(adultsOption).toBeVisible();
  483 |     await expect(bothOption).toBeVisible();
  484 |
  485 |     // 4.2 Check hover state on dropdown option
  486 |     await adultsOption.hover();
  487 |     const hoverBg = await adultsOption.evaluate(el => window.getComputedStyle(el).color);
  488 |     expect(hoverBg).not.toBe('rgba(0, 0, 0, 0)'); // Should change on hover
  489 |
  490 |     // 5. Collapse dropdown by clicking outside
  491 |     if (!isMobile) {
  492 |         await page.click('body', { position: { x: 0, y: 0 } });
  493 |         await expect(dropdownPanel).toBeHidden();
  494 |     }
  495 | });
  496 |
  497 |
  498 |
  499 | test('TC015_@sanity_Clinical Trial Landing Page_Validate the working of Age group dropdown filter on clinical trial landing page in desktop view/iPad view, mobile view', async ({ page, beforeHook, afterHook, homepage, clinical_Trial_Landing_Page, testData, isMobile }) => {
  500 |     // 1. Navigate to Clinical Trial Landing Page
  501 |     await homepage.navigateToClinicalTrialLandingPage();
  502 |     await expect(page.url()).toContain("/clinical-trials");
  503 |
  504 |     // Open filter panel on mobile if needed
  505 |     if (isMobile) {
  506 |         await clinical_Trial_Landing_Page.filterResults_mobile.click();
  507 |     }
  508 |
  509 |     // Validate "Adults Age Group"
  510 |     await clinical_Trial_Landing_Page.validateAgeGroupFilter('Adults');
  511 |
  512 |     // Optionally reset filters if needed
  513 |     if (!isMobile) {
  514 |         await clinical_Trial_Landing_Page.clickOnCloseButton_resetFilter();
  515 |     } else {
  516 |         await clinical_Trial_Landing_Page.filter_results_1.click();
  517 |         await clinical_Trial_Landing_Page.clearfilters_mobile.click();
  518 |         await clinical_Trial_Landing_Page.filterResults_mobile.click();
  519 |     }
  520 |
  521 |     // Validate "Both Age Group"
  522 |     await page.waitForTimeout(3000);
  523 |     await clinical_Trial_Landing_Page.validateAgeGroupFilter('Both');
  524 | });
  525 |
  526 |
  527 |
  528 |
  529 | test('TC016_@sanity_Clinical Trial Landing Page_Validate the UI of View BY filter on clinical trial landing page in desktop view/iPad view/mobile view', async ({ page, beforeHook, afterHook, homepage, clinical_Trial_Landing_Page, testData, isMobile }) => {
  530 |     // 1. Navigate to Clinical Trial Landing Page
  531 |     await homepage.navigateToClinicalTrialLandingPage();
  532 |     await expect(page.url()).toContain("/clinical-trials");
  533 |
  534 |     // Open filter panel on mobile if needed
  535 |     if (isMobile) {
  536 |         await clinical_Trial_Landing_Page.filterResults_mobile.click();
```